# PostgreSQL Configuration
# Copy this file to .env and update with your values

POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# Database Connection (for Spring Boot)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USERNAME=postgres
DB_PASSWORD=your_password_here

# Application Database User (optional - for separate app user)
# If you want to use a separate user for the application, uncomment and set:
# DB_APP_USER=optimal_choice_user
# DB_APP_PASSWORD=your_app_password_here
# Then update application.yaml to use DB_APP_USER and DB_APP_PASSWORD
